//   const beatIdeaAsText = <Typography>{beatIdea}</Typography>;

//   const quickResetRandomiser = (randomsToReset: Array<RandomiseMe>) => {
//     if (randomsToReset.includes('beatIdea')) setBeatIdea("Add a kick on the 'e' of 1");
//     // if (randomsToReset.includes('startFill')) setStartFillKey(startFillKey + 1);
//     // if (randomsToReset.includes('fill')) setFillKey(fillKey + 1);
//     // if (randomsToReset.includes('tempo')) setTempoKey(tempoKey + 1);
//   };

// <Fade in={checked}>{beatIdeaAsText}</Fade>

const QuickRandomiser = () => {
  return <div>hello</div>;
};

export default QuickRandomiser;
